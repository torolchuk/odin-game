package entities

import sdl "vendor:sdl2"

@(private)
UpdatePlayerEntity :: proc(entity: Entity, game_state: ^main.GameState) {

}

@(private)
RenderPlayerEntity :: proc(renderer: ^sdl.Renderer, entity: ^Entity) {

}